const componentesSchema = require('./componentes.schema')
const fabricantesSchema = require('./fabricantes.schema')
const productosSchema = require('./productos.schema')

module.exports = {componentesSchema, fabricantesSchema, productosSchema }